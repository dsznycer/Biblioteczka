import 'package:flutter/material.dart';

class BookAdd extends StatelessWidget {
  const BookAdd({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      height: 300,
      width: 300,
      child: Column(
        children: [Text('Dodaj ksiązkę')],
      ),
    );
  }
}
